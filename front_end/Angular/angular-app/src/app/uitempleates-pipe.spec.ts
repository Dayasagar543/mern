import { UItempleatesPipe } from './uitempleates-pipe';

describe('UItempleatesPipe', () => {
  it('create an instance', () => {
    const pipe = new UItempleatesPipe();
    expect(pipe).toBeTruthy();
  });
});
